Summ.Level = function (number, lines, rows, keys, figures) {
    this.number = number;
    this.figures = figures;
    this.lines = lines;
    this.rows = rows;
    this.keys = keys;
};

Summ.Level.prototype = {};